for f in ~/.config/zsh/conf.d/*.zsh; do source "$f"; done

# Avoid gnome-ssh-askpass: its Wayland keyboard grab deadlocks with GNOME
# Shell's "wants to inhibit shortcuts" modal dialog.
export SSH_ASKPASS="$HOME/.local/bin/zenity-ssh-askpass"
